.git
.gitignore
README.md

__pycache__/
*.pyc
.venv/
venv/

.env
.env.example

# NOTE: data/base_index/ is intentionally NOT ignored here — we commit
# the prebuilt index to git and want it baked into the Docker image, so
# a fresh Render deploy has a working chatbot without a rebuild step.
data/*.db
data/*.sqlite3